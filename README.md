# personal-home-page

Source for [umutcelik.com.tr](https://umutcelik.com.tr). Single-page static site, no build step, deployed via GitHub Pages on push to `main`.

## Local preview

```bash
python3 -m http.server 8000
# open http://localhost:8000
```

See `CLAUDE.md` for deploy/DNS notes and content rules.
